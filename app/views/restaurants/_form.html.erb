<%= simple_form_for(@restaurant) do |f| %>
  <%= f.input :name %>
  <%= f.input :address %>
  <%= f.input :phone_number %>
  <%= f.input :category, as: :select, collection: %w(chinese french belgian italian japanese)  %>
  // TODO Create a variable in the Model
  <%= f.submit class: 'btn btn-primary' %>
<% end %>
